apiVersion: v1
kind: resource
metadata:
  name: resource
  labels:
    project: roboshop
    environment: dev
    purpose: poc
spec:
 containers:
 - name: nginx
   image: nginx
resources:
  requests:
    memory: "128Mi"
    cpu: "100m"
  limits:
    memory: "256Mi"
    cpu: "150m"